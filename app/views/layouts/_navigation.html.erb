<ul class="nav">
  <li> <%= link_to "Postit!", root_path, :class => 'brand' %> </li>
  <li> <%= link_to 'New Post', new_post_path %> </li>
  <li>
    <div class='nav_item' id='navcatdropdown'>
      <a class='btn btn-small dropdown-toggle' data-toggle='dropdown' href="#">
        Categories
        <i class='caret'></i>
      </a>
      <ul class='dropdown-menu'>
        <% @categories.each do |category| %>
          <li> <%= link_to category.name, category_path(category) %> </li>
        <% end %>
        <li class='divider'></li>
        <li>
          <%= link_to(new_category_path) do %>
            <i class='icon-plus'></i>
            New Category
          <% end %>
        </li>
      </ul>
    </div>
  </li>
  <li>
    <div class:'nav_item'>
      <%= link_to 'Register', root_path, class: 'btn btn-small btn-success' %>
    </div>
  </li>
  <li>
    <div class='nav_item'>
      <%= link_to 'Login', root_path, class: 'btn btn-small' %>
    </div>
  </li>
</ul>

